import { PrefixRemoveSpacePipe } from './prefix-remove-space.pipe';

describe('PrefixRemoveSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new PrefixRemoveSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
