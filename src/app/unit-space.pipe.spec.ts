import { UnitSpacePipe } from './unit-space.pipe';

describe('UnitSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new UnitSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
