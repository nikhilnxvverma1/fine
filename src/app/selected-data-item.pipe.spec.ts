import { SelectedDataItemPipe } from './selected-data-item.pipe';

describe('SelectedDataItemPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedDataItemPipe();
    expect(pipe).toBeTruthy();
  });
});
